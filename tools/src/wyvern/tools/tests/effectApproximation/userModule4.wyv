module def userModule4(file : effectApproximation.File)

// Importing an object, not a type
import effectApproximation.logger

def id(x : String) : String
  val myLogger = logger(file)
  myLogger.update(x)
  x
